// Domain models are documented here for Codex.
// v0.2 uses plain JavaScript for easy local setup.

/**
 * Stock:
 * {
 *   symbol: string,
 *   name: string,
 *   price: number,
 *   source: string
 * }
 */

/**
 * Warrant:
 * {
 *   symbol: string,
 *   name: string,
 *   underlying: string,
 *   underlyingPrice: number,
 *   strikePrice: number,
 *   ratio: number,
 *   price: number,
 *   issuer: string,
 *   expiryDate: string,
 *   type: "CALL"
 * }
 */
